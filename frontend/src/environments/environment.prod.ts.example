// Template only. The real environment.prod.ts is generated by scripts/set-env.js
// from the API_URL env var on every `npm run build`. Do not commit a real file.
export const environment = {
  production: true,
  apiUrl: 'https://your-backend-host.example.com/api',
};
